#ifndef clearvm_common_h
#define clearvm_common_h

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#define UNUSED(x) (void)(x)

typedef enum {

    RESULT_OK,
    RESULT_ERR

} Result;

#endif
