
// TODO: Actual tests !!!

struct DoesStuff {
    num x;
    func doStuff() {
        print "Doing stuff...";
    }
    func makeNumber() num {
        return 3.2;
    }
}

val doer = DoesStuff(3.14);
print "Value = " + str(doer.x);
doer.doStuff();
print "Value + Number = " + str(doer.x + doer.makeNumber());
