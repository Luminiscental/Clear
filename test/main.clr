
// Simple test program:

{
    val i = "happens in a scope";
    {
        val i = "gets shadowed ~~ooo~~";

        val i = 1i; // replaced
        val f = i;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        val i = i + 1i;
        val f = i * f;

        print str(i) + "! = " + str(f);
        print;
    }
    print i;
}

val x = 3.7;
print "x = " + str(x);

print;

val j = -293502935i;
print "j = " + str(j);

print;

val y = 348.524 - 9204924.282326;
print "y = " + str(y);

print;

val tasty = true;
val notTasty = false;

print "tasty = " + str(tasty);
print "notTasty = " + str(notTasty);
print "tasty == !notTasty = " + str(tasty == !notTasty);

print;

val something = 0;
print str(something) + " is " + str(bool(something));

print;

val empty = "";
print """" + empty + """ is " + str(bool(empty));

print;

val notEmpty = "hello";
print """" + notEmpty + """ is " + str(bool(notEmpty));

print;

print "2.3 < 2.4 = " + str(2.3 < 2.4);
print "2.3 == 1.2 + 1.1 = " + str(2.3 == 1.2 + 1.1);
print "-1.7 <= -1.5 - 0.2 = " + str(-1.7 <= -1.5 - 0.2);

