
struct Point {
    int x;
    int y;
}

struct Rectangle {
    Point a;
    Point b;
}

func test(Point a, Rectangle b) int {
    return a.x + b.y + b.a.x + b.a.y + b.b.x + b.b.y;
}

val a = Point(1i, 2i);
val b = Point(2i, 3i);
val rect = Rectangle(a, b);

print test(a, rect);
