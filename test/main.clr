
val cond = true or false;
{
    val inside = "inside";
    {
        val deeper = 12i;
        print "deeper = " + str(deeper);
        print "inside = " + str(inside);
        print "cond = " + str(cond);
    }
    print "inside = " + str(inside);
    print "cond = " + str(cond);
}
print "cond = " + str(cond);
