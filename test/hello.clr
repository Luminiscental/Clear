
val a = fac(b);
val b = 2i;
val c = maybe2(false);

val y = x;
val x = y;

print x;

func fac(int n) int {
    if (n == 0i) {
        return 1i;
    }
    return n * fac(n - 1i);
}

func maybe2(bool input) int {
    if (input) {
        return 2i;
    }
    {
        val x = 3;
    }
}
